# OpenAI 圖像生成器 UI - 依賴包

# 核心依賴
openai>=1.0.0
requests>=2.25.0
Pillow>=8.0.0

# Tkinter 版本依賴
# Tkinter 通常隨 Python 一起安裝，無需額外安裝

# Streamlit 版本依賴
streamlit>=1.28.0

# Gradio 版本依賴
gradio>=4.0.0

# 可選依賴（用於圖像處理）
opencv-python>=4.5.0
numpy>=1.21.0

# 開發依賴（可選）
python-dotenv>=0.19.0  # 用於環境變量管理
